services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: snp500
      POSTGRES_PASSWORD: snp500
      POSTGRES_DB: snp500
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: {}
